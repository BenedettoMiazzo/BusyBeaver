version: '3.8'
services:
  busybeaver-dev:
    build: .
    container_name: busybeaver-ubuntu
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: /bin/bash